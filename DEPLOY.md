# Aivinci Studios — built site

This branch holds the production export (relative asset paths) ready to be served as-is:
Hostinger hPanel → Advanced → Git → repository `https://github.com/gireeshkumarreddy/Aivinci.git`, branch `hostinger`, directory `public_html`.
Rebuild from `main` with `npm run build:static`.
